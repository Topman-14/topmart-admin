import { Syne, Montserrat, Comfortaa, Jost, Raleway } from "next/font/google";

export const syne = Syne({ subsets: ["latin"] });

export const montserrat = Montserrat({ subsets: ["latin"] });

export const comfortaa = Comfortaa({ subsets: ["latin"] });

export const jost = Jost({ subsets: ["latin"] });

export const raleway = Raleway({ subsets: ["latin"] });